# Watch TikTok Without App

A browser-based TikTok link cleaner and official short-link expander.

The app cleans recognized full TikTok URLs locally and uses a restricted server endpoint to expand official TikTok short links.

## Development

```bash
npm install
npm run dev
```

## Validation

```bash
npm run typecheck
npm run audit
npm run build
```
